height := (((1+2)/(3*5) + _barc * SUBCALL multiplyArray(0)) + 5 * ((3+1)/2));
height := (((1+2)/(3*5) + _barc * SUBCALL multiplyArray(0, (5+3))) + 5 * ((3+1)/2));

PROGRAM prog1;

VAR height : INT;
VAR width : INT;
VAR idades : VECTOR[2..0];

FUNCTION multArray(VECTOR[1..0] multiArray, INT multiplier) ->
  	width := 2;  

ENDFUNC

BEGIN
            height := 1;
                  
END 
